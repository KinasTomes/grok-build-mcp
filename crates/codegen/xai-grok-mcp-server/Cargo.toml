[package]
name = "xai-grok-mcp-server"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Standalone MCP gateway for the Grok Build local tool runtime"

[dependencies]
anyhow = { workspace = true }
rmcp = { version = "2.1", default-features = false, features = [
    "server",
    "transport-io",
] }
serde_json = { workspace = true }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
xai-grok-sandbox = { path = "../xai-grok-sandbox" }
xai-grok-tools = { workspace = true }

[dev-dependencies]
rmcp = { version = "2.1", default-features = false, features = [
    "client",
    "server",
    "transport-async-rw",
] }
tempfile = { workspace = true }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }

[lints]
workspace = true
